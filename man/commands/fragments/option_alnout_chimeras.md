`--alnout` *filename*
: Write multi-way chimera alignments to *filename* in a human-readable
  format (see the example in the DESCRIPTION). Use `--alignwidth` to
  set the alignment width (default 60 nucleotides).
