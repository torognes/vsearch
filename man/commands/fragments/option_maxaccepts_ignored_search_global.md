`--maxaccepts` *positive integer*
: Option is ignored: every database sequence is aligned, with no early
  termination. Accepted so that a command line written for
  `--usearch_global` can be reused unchanged; use `--maxhits` to limit
  how many hits are *reported*.
