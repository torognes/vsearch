`--fastqout` *filename*
: Write sequences passing the filter to *filename*, in fastq format
  (see [`vsearch-fastq(5)`](../formats/vsearch-fastq.5.md)). Requires
  fastq input. When `--reverse` is used, only forward reads are
  written here.
