`--fastqout` *filename*
: Write scrambled sequences to *filename*, in fastq format (see
  [`vsearch-fastq(5)`](../formats/vsearch-fastq.5.md)). Requires fastq
  input. Quality strings are copied through unchanged (see the
  DESCRIPTION section).
