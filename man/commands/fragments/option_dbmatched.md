`--dbmatched` *filename*
: Write database target sequences that match at least one query sequence to
  *filename*, in fasta format. If `--sizeout` is specified, the number of
  matching queries is annotated in the header (`;size=integer`).
