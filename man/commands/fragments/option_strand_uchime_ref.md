`--strand` *plus*
: Check the *plus* strand only. This is the only accepted value:
  `--uchime_ref` rejects `--strand both` with a fatal error. The
  option is accepted for compatibility with usearch. Keywords are
  case-insensitive.
