`--tabbedout` *filename*
: Write orientation results to *filename* as a tab-separated file with
  four columns:

    1. query label;
    2. orientation (+, -, or ?);
    3. number of matching words on the forward strand;
    4. number of matching words on the reverse-complementary strand.
