`--wordlength` *positive integer*
: Option is ignored: `--search_global` builds no *k*-mer index. A
  preformatted UDB database is still accepted, but only its sequences
  are read; the index it carries is not loaded. Accepted so that a
  command line written for `--usearch_global` can be reused unchanged.
