`--minwordmatches` *non-negative integer*
: Option is ignored: `--search_global` has no *k*-mer pre-filtering
  stage, so there is no word threshold for a target to clear. Accepted
  so that a command line written for `--usearch_global` can be reused
  unchanged.
