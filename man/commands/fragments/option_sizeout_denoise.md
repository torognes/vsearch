`--sizeout`
: Add abundance annotations (`;size=integer`) to the headers of the
  denoised sequences written with `--fastaout`: the number of reads
  corrected to that sequence. Headers of the reads written with
  `--fastqout` are not modified.
