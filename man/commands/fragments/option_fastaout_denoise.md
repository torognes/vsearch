`--fastaout` *filename*
: Write the denoised sequences (the centers of the partitions) to
  *filename*, in fasta format, in their order of creation. Each entry
  bears the header of the first read seen with that sequence. Use
  `--sizeout` to add the number of reads corrected to each sequence.
