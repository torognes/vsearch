`--mid` *real*
: Reject the sequence match if the pairwise identity, computed
  ignoring all gaps (internal and terminal), is lower than *real*,
  a percentage from 0 to 100 (for example, `--mid 97` requires 97%
  identity; note that the other identity options use fractions).
