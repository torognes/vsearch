`--join_padgap` *string*
: Use *string* as the sequence padding inserted between the forward and
  reverse reads. The default is `NNNNNNNN` (eight N's). Accepts an
  empty string, or any other combination of ASCII characters.
