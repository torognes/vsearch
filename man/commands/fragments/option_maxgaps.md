`--maxgaps` *positive integer*
: Reject the sequence match if the alignment contains more than
  *integer* gap openings: a run of consecutive gap columns counts as
  one, whatever its length (use `--maxdiffs` to bound the number of
  gap columns).
