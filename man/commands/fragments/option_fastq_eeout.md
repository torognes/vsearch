`--fastq_eeout`
: Add the expected error count to each sequence header in output fasta
  or fastq files, as the annotation `;ee=float`. Synonym of
  `--eeout`. Use `--xee` to remove this annotation from headers. See
  [`vsearch-expected_error(7)`](../misc/vsearch-expected_error.7.md).
