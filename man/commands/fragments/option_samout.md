`--samout` *filename*
: Write alignment results to *filename* in the SAM format. Use
  `--samheader` to include header lines. Each non-header line is a SAM
  record representing either a query-target alignment or the absence
  of a match.
