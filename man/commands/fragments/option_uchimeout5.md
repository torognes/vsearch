`--uchimeout5`
: When using `--uchimeout`, write chimera detection results using a
  17-field, tab-separated uchime-like format. This drops the 5th
  field (top parent T) from the standard `--uchimeout` format, for
  compatibility with usearch version 5 and earlier versions.
