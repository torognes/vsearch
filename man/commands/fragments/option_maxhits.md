`--maxhits` *non-negative integer*
: Set the maximum number of hits to report once the search is
  terminated for a given query; hits are sorted by decreasing
  identity. Unlimited by default, or when the argument is zero. When
  searching both strands, `--maxhits` controls the total number of
  hits reported per query across both strands.
