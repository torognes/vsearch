`--maxid` *real*
: Reject the sequence match if the pairwise identity between the two
  sequences is greater than *real*, a fraction from 0.0 to 1.0 (for
  example, `--maxid 0.97` rejects matches above 97% identity).
