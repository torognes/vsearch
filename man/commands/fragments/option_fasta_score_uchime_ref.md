`--fasta_score`
: Add the chimera score to the sequence headers in the fasta output
  files for chimeras, non-chimeras, and borderline sequences, using
  the format `;uchime_ref=float` (appended at the end of the header).
