`--clusterout_sort`
: Sort some output files by decreasing abundance instead of input
  order. Applies to `--consout`, `--msaout`, `--profile`,
  `--centroids`, and `--uc`. For `--uc`, the sorting applies only to
  the centroid information part (the C lines).
