`--output_no_hits`
: Write both matching and non-matching queries to `--alnout`,
  `--blast6out`, `--samout`, or `--userout` output files.
  Non-matching queries are labelled 'No hits' in `--alnout` files.
